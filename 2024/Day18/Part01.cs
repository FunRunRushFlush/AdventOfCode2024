namespace Day18;
public class Part01
{
    private char[,] MemoryField;
    private int MaxAllowedBytes = 1024;
    public long Result(ReadOnlySpan<string> input)
    {
        InputParser(input);
    }

    private void InputParser(ReadOnlySpan<string> input)
    {
        for (int i = 0; i < MaxAllowedBytes; i++)
        {

        }


    }
}